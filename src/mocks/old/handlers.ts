// import type { Mock, Mask, RequestHandlerFn } from './types'

// export function getHandlers(mocks: Mock[]) {
//   const paths: Mask[] = []
//   const handlers: RequestHandlerFn[] = []
//   mocks.forEach((mock) => {
//     paths.push(mock.path)
//     handlers.push(mock.mockFn)
//   })

//   return { paths, handlers }
// }
